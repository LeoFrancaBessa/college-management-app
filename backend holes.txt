Feito / Concluído
- RF-40 Backup/Export JSON — Prioridade Alta — FEITO (f44dfd7) — specs/04-funcionalidades.md:176 marcado [x]. GET /api/v1/export + POST /api/v1/import em backend/app/api/v1/export.py:15 com backend/app/services/export_service.py:94 (build_export) e :144 (import_data) dumpando Period+Course+Board+Item+Tag+ItemType+features+item_tags.
- Autenticação imposta — RNF Segurança — FEITO — todas as rotas de negócio agora exigem `get_current_user` (router-level `dependencies=[Depends(get_current_user)]`) em: periods, courses, items, item-types, boards, schedule, tags, trash, ai, export/import. Verificado: sem token → 401 em 10 rotas; com token/Bearer/cookie → 200; `/health`, `/auth/register`, `/auth/login`, `/auth/logout-all` permanecem públicos. Testes em backend/tests/conftest.py atualizados com fixtures `client` (autenticado via dummy User) + `unauth_client` (22 testes passando em Python 3.12 — o Dockerfile já usa python:3.12-slim; Python 3.9 local não suporta `X|Y` sem `from __future__ import annotations`).
- RF-21 Média ponderada da Cadeira — Prioridade Média — FEITO — specs/04-funcionalidades.md:93 marcado [x]. GET /api/v1/courses/{id}/average em backend/app/api/v1/courses.py:19 com backend/app/services/course_service.py:79 (get_course_average) calculando sum(score*weight)/sum(weight) apenas em itens ACTIVE com features.grade.score != null. Schema CourseAverageRead em backend/app/schemas/course.py. Shape canônico grade={score, max_score=10, weight=1} (alias nota={nota_obtida, peso} tolerado). Validado: vazio→average null, ponderada 8.5*2+7*1/3=8.0, arquivado ignorado, 404 em curso inexistente, weight<=0 fallback 1.
- RF-19 Feature Anexos — Prioridade Média — FEITO — specs/04-funcionalidades.md:85 marcado [x]. Tabela attachments(id,item_id,original_filename,stored_filename,content_type,size,path,created_at) em backend/app/models/attachment.py + Item.attachments relationship backend/app/models/item.py:71 + migração b7c8d9e0f1a2_create_attachments.py + schema AttachmentRead backend/app/schemas/attachment.py + config ATTACHMENTS_DIR="/app/attachments" / MAX_ATTACHMENT_SIZE=20MB backend/app/core/config.py:24 + volume attachments:/app/attachments docker-compose.yml:27. Endpoints em backend/app/api/v1/attachments.py:25 (router-level auth): POST /items/{id}/attachments (multipart, any mime, 20 MB, basename traversal guard, uuid stored_filename, streaming 1 MB chunks, empty file rejeitado), GET /items/{id}/attachments, GET /attachments/{id} (FileResponse streaming + Content-Disposition), DELETE /attachments/{id} (row + unlink). Serviço backend/app/services/attachment_service.py. Export/import em backend/app/services/export_service.py (ATTACHMENT_FIELDS + build_export + import_data com DELETE/INSERT attachments + _reset_sequences; backward compat quando payload sem chave attachments; bytes ficam no volume — backup infra via pg_dump+tar). Limpeza física em cascata: delete_item (subtree ids → Attachment.path → unlink), delete_course, delete_period e expire_trash (RF-39) em backend/app/services/item_service.py, course_service.py, period_service.py, trash_service.py. Validado: upload 201 + stored_filename uuid, list/download/delete, 401 sem auth, delete de Item/Curso/Período remove arquivos do disco, export roundtrip com attachments.
- RF-20 Feature Recorrência — Prioridade Alta — FEITO — specs/04-funcionalidades.md:89 marcado [x]; specs/features/cronograma.md atualizado (Fora de escopo / Modelo / Regras / Critérios). Novo backend/app/services/recurrence.py: validate_recurrence (frequency daily|weekly|monthly|yearly, interval >=1 default 1 max 366, weekdays [0..6] só com weekly, exatamente um de until|count, requires due_date, until >= due_date com _ensure_aware, count <=500 MAX_COUNT, normaliza para {frequency,interval,weekdays?,until|count} com until em ISO) + expand_recurrence (due_date é ocorrência 0 com horário preservado, until inclusivo, daily/weekly/weekly+weekdays com bloco interval semanas/_add_months com clamp, yearly, MAX_ITER 1000, _ensure_aware para due_date/from/to, janela inclusiva; âncora fora da janela ainda expande ocorrência dentro) + _next_occurrence/_add_months/_parse_dt. Integrado em backend/app/services/item_service.py:create_item (valida se recurrence em features, normaliza) e update_item (revalida ao mudar features ou due_date: patch features com recurrence, due_date null com recurrence ativo, due_date alterado com recurrence existente; normaliza until ISO; desativa com features {}). Cronograma virtual em backend/app/services/schedule_service.py:40 (_has_recurrence), :52 (_occurrence_proxy via SimpleNamespace), :90 list_schedule (fetch sem filtro de data para recorrências, expand_recurrence por item, fallback para recorrência corrompida, _ensure_aware em anchor/from/to, ordenação due_date ASC id ASC via proxy) e get_homepage (_ensure_aware now, janela [today 00:00, +7d 23:59 UTC]). Sem tabela extra (Regra 2), sem migração — features JSON já coberto por export/import. Validado: create 400 sem due_date / sem until+count / ambos / weekdays fora de weekly / count 0 / count >500 / interval 0 / until < due_date / patch due_date null com recurrence; create 201 normalizado interval 1; schedule expande daily 3, weekly 3, weekly+weekdays seg/qua 4 (05+07+12+14/01), biweekly interval 2 seg (05+19/01+02/02), monthly clamp 31/01→28/02→28/03, yearly 2, daily until 01-03/01, janela 02/02 isolada e janela cruzada 13-20/01→15/01, multi-cadeira curso filter, homepage 7d com recorrência, trash/archive e sem due_date exclusão, desativação com features {} volta a item simples; 22 testes existentes continuam passando.

Falta / incompleto — bloqueia MVP
1. Filtro arquivado em listagens — Regra 6 UC-01/02/03: list_periods backend/app/services/period_service.py:17, list_courses backend/app/services/course_service.py:28, list_items backend/app/services/item_service.py:92 retornam ACTIVE+ARCHIVED+TRASH sem ?status/?include_archived. Contraria "some das listas ativas".
2. Infra dev: CORSMiddleware ausente em backend/app/main.py:35 (quebra dev Vite 5173 -> 8000); COOKIE_KWARGS secure=False backend/app/api/v1/auth.py:25 ok dev mas rever para prod SameSite=Strict previsto em docs/architecture.md:31.

RF-16 Nota, RF-17 Checklist, RF-18 Anotações estão funcionalmente cobertos pelo Item.features JSON genérico (PATCH /items/{id} backend/app/schemas/item.py:24), mas sem validação de schema (ex.: nota_obtida/max/peso). Considerado OK pro MVP se RF-21 for entregue; ideal seria validar com Pydantic.

Ordem sugerida p/ fechar: Filtro arquivado + CORS.
