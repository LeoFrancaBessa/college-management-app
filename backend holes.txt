Feito / Concluído
- RF-40 Backup/Export JSON — Prioridade Alta — FEITO (f44dfd7) — specs/04-funcionalidades.md:176 marcado [x]. GET /api/v1/export + POST /api/v1/import em backend/app/api/v1/export.py:15 com backend/app/services/export_service.py:94 (build_export) e :144 (import_data) dumpando Period+Course+Board+Item+Tag+ItemType+features+item_tags.
- Autenticação imposta — RNF Segurança — FEITO — todas as rotas de negócio agora exigem `get_current_user` (router-level `dependencies=[Depends(get_current_user)]`) em: periods, courses, items, item-types, boards, schedule, tags, trash, ai, export/import. Verificado: sem token → 401 em 10 rotas; com token/Bearer/cookie → 200; `/health`, `/auth/register`, `/auth/login`, `/auth/logout-all` permanecem públicos. Testes em backend/tests/conftest.py atualizados com fixtures `client` (autenticado via dummy User) + `unauth_client` (22 testes passando em Python 3.12 — o Dockerfile já usa python:3.12-slim; Python 3.9 local não suporta `X|Y` sem `from __future__ import annotations`).

Falta / incompleto — bloqueia MVP
1. RF-19 Feature Anexos — Prioridade Média — 0% specs/05-modelo-de-dominio.md:83, docs/architecture.md:25. Arquitetura prevê tabela própria Attachment (binário não cabe em JSONB) + volume attachments:/app/attachments docker-compose.yml:27, mas não existe backend/app/models/attachment.py (só Item.features JSON backend/app/models/item.py:42). Falta: model attachments(id,item_id,filename,mimetype,size,path), POST /items/{id}/attachments (multipart), GET /attachments/{id}, DELETE, serviço de disco + Caddy static.
2. RF-21 Média ponderada da Cadeira — Prioridade Média — 0% specs/04-funcionalidades.md:93, UC-10. Nenhum grep average/peso no backend. Falta GET /courses/{id}/average (ou campo em CourseRead) calculando sum(nota*peso)/sum(peso) só em itens com features.nota preenchida (specs/05-modelo-de-dominio.md:68).
3. RF-20 Recorrência — Prioridade Alta — ~10% specs/05-modelo-de-dominio.md:87. features aceita JSON genérico, mas não há validação frequencia + termino nem expansão no cronograma. schedule_service.py:42 tem TODO(RF-20) correto e retorna só due_date base (specs/features/cronograma.md:53).
4. Filtro arquivado em listagens — Regra 6 UC-01/02/03: list_periods backend/app/services/period_service.py:17, list_courses backend/app/services/course_service.py:28, list_items backend/app/services/item_service.py:92 retornam ACTIVE+ARCHIVED+TRASH sem ?status/?include_archived. Contraria "some das listas ativas".
5. Infra dev: CORSMiddleware ausente em backend/app/main.py:35 (quebra dev Vite 5173 -> 8000); COOKIE_KWARGS secure=False backend/app/api/v1/auth.py:25 ok dev mas rever para prod SameSite=Strict previsto em docs/architecture.md:31.

RF-16 Nota, RF-17 Checklist, RF-18 Anotações estão funcionalmente cobertos pelo Item.features JSON genérico (PATCH /items/{id} backend/app/schemas/item.py:24), mas sem validação de schema (ex.: nota_obtida/max/peso). Considerado OK pro MVP se RF-21 for entregue; ideal seria validar com Pydantic.

Ordem sugerida p/ fechar: RF-21 > RF-19 > RF-20 + Filtro arquivado + CORS.
