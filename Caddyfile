{$DOMAIN:localhost} {
	encode gzip

	# Backend API (FastAPI)
	handle /api/* {
		reverse_proxy backend:8000
	}

	# Frontend (static build served by the nginx container)
	handle {
		reverse_proxy frontend:80
	}
}
