server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: any unknown route falls back to index.html (React Router takes it from there)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
